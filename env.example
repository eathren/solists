# If you have issues with keys, remember .env values are space-sensitive. 
# so make sure there isn't any whitespace after the = and before your key item!

SECRET_KEY='STRING'

DEBUG=True


DATABASE_NAME=STRING
DATABASE_USER=STRING
DATABASE_PASSWORD=STRING
DATABASE_HOST=127.0.0.1
DATABASE_PORT=5432



STRIPE_TEST_PUBLISHABLE_KEY=STRING
STRIPE_TEST_SECRET_KEY=STRING